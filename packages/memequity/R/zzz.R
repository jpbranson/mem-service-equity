# data.table is used with its [ syntax inside this package.
.datatable.aware <- TRUE

utils::globalVariables(c("i", "j", "g", "cx", "cy", "tb", "tb0", "ax", "ay", "bx", "by",
                         "at", "bt", "dist", "lag", ":="))
