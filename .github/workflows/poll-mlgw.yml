name: poll-mlgw

# Phase 1 collection (plan 6.2). Outage snapshots every 5 minutes; runs of
# 140 minutes start every 2 hours so they overlap. The plan requires six
# months of history including a significant weather event before the MLGW
# panel can publish.

on:
  schedule:
    - cron: "30 */2 * * *"
  workflow_dispatch:
    inputs:
      minutes:
        description: "Minutes to poll"
        default: "140"

permissions:
  contents: write

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 160
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pip install -r pollers/requirements.txt
      - name: Poll
        working-directory: pollers
        run: python mlgw_poller.py --minutes "${{ github.event.inputs.minutes || '140' }}" --out ../data/poller
      - name: Archive to weekly release
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash pollers/archive_release.sh mlgw data/poller/mlgw
